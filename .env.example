# Local prototype (SQLite).
DATABASE_URL="file:./prisma/dev.db"

# On Render the Blueprint sets: file:/tmp/swishit.db
# (writable path — deploy filesystem is often read-only)

# Google Analytics 4 — get this from analytics.google.com (Admin → Data streams)
# Copy this file to .env.local and paste your real G-XXXXXXXXXX id.
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-8JBGC2C9HX 
npm run dev

